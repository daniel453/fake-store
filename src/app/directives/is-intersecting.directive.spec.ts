import { IsIntersectingDirective } from './is-intersecting.directive';

describe('IsIntersectingDirective', () => {
  it('should create an instance', () => {
    const directive = new IsIntersectingDirective();
    expect(directive).toBeTruthy();
  });
});
